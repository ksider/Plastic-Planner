<!doctype html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>Compounding Experiments</title>
    <link rel="stylesheet" href="/public/styles.css" />
    <script defer src="/public/app.js"></script>
  </head>
  <body>
    <header>
      <div class="header-line">
        <a class="back-link" href="#" data-back-link>←</a>
        <h1>Compounding Experiments</h1>
      </div>
      <%- include('partials/nav', { active: 'compounding' }) %>
    </header>
    <main>
      <div class="card">
        <div style="display:flex; justify-content:space-between; align-items:center;">
          <h2>Experiments</h2>
          <a class="btn" href="/experiments/new">New Compounding Experiment</a>
        </div>
        <% if (experiments.length === 0) { %>
          <p>No experiments yet. Create one.</p>
        <% } else { %>
          <div class="table-wrap">
            <table>
              <thead>
                <tr>
                  <th>Name</th>
                  <th>Final Mass (g)</th>
                  <th>Seed</th>
                  <th>Notes</th>
                  <th>Created</th>
                </tr>
              </thead>
              <tbody>
                <% experiments.forEach((exp) => { %>
                  <tr>
                    <td><a href="/experiments/<%= exp.id %>"><%= exp.name %></a></td>
                    <td><%= exp.final_mass_g %></td>
                    <td><%= exp.seed %></td>
                    <td><%= exp.notes || '' %></td>
                    <td><%= exp.created_at %></td>
                  </tr>
                <% }) %>
              </tbody>
            </table>
          </div>
        <% } %>
      </div>
    </main>
  </body>
</html>
